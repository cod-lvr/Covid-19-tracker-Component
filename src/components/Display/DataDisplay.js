import React from 'react';
import { Fragment } from 'react-is';
import styles from './DataDisplay.moddule.css';

const DataDisplay = props => {
    return (
        <Fragment>
            <seaction className={styles.boxes}>
                
            </seaction>
        </Fragment>
    )
}

export default DataDisplay;